# Audio outputs

Usable Sonia voice renders live beside the novel sections, organized by chapter.

- `section-*.mp3` and `section-*.m4a` — current renders for the corresponding novel sections

The TTS virtual environment and intermediate segment files are kept outside the repository because they are tooling and temporary render data.
